<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>

        <%- messages() %>

            <div class="account-management">
                <h2>Welcome <%= locals.accountData.account_firstname %>!</h2>

                <p>
                    
                    You're logged in!
                    
                </p>

                
            </div>